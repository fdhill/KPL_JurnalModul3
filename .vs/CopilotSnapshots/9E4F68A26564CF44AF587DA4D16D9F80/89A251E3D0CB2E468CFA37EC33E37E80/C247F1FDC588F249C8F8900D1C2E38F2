using modul3_103082400012;
using System.Linq.Expressions;

public class MyClass
{
    KodeBuah kodeBuah = new KodeBuah();
    PosisiKarakterGame posisiKarakterGame = new PosisiKarakterGame();

    static void main(String[] args)
    {
        Console.WriteLine("Masukkan angka: \n 1. Kode Buah \n 2. Posisi Karakter Game");
        int pilihan = Int16.Parse(Console.ReadLine());

        switch (pilihan)
        {
            case 1:
                // code block
                break;
            case 2:
                // code block
                break;
            default:
                Console.WriteLine("Error");
                break;
        }
    }

    public void game1()
    {
        while (true)
        {
            Console.Write("Massukan nama buah untuk menemukan kode buah (type stop for closing program): ");
            String input = Console.ReadLine();

            if (input.Equals("stop"))
            {
                return;
            }

            kodeBuah.getKodeBuah(input);
        }
    }

    public void game2()
    {
        while (true)
        {
            Console.Write("Massukan perintah untuk menggerakan karakter (type stop for closing program): ");
            String input = Console.ReadLine();

            if (input.Equals("stop"))
            {
                return;
            }

            posisiKarakterGame.command(input);
        }
    }
}